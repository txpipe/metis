name: Check Catalog

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/check_catalog.yml"
      - "catalog/**"
      - "frontends/catalog/**"
      - "skills/**"
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/check_catalog.yml"
      - "catalog/**"
      - "frontends/catalog/**"
      - "skills/**"

jobs:
  check:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontends/catalog
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'
          cache-dependency-path: frontends/catalog/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify generated skill catalog
        working-directory: .
        run: node catalog/scripts/generate-skill-catalog.mjs

      - name: Run typecheck
        run: pnpm run typecheck

      - name: Run lint
        run: pnpm run lint
