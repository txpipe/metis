{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "title": "Cardano Relay Helm Values",
  "description": "Supernode-opinionated Cardano relay chart values. This schema is the public configuration interface for LLMs and MCP clients; pass values in this shape directly to Helm.",
  "type": "object",
  "required": ["node", "persistence"],
  "additionalProperties": false,
  "properties": {
    "nameOverride": {
      "type": "string",
      "description": "Advanced, usually not modified. Overrides the Kubernetes app name used by the chart.",
      "default": "",
      "x-supernodeCategory": "advanced"
    },
    "fullnameOverride": {
      "type": "string",
      "description": "Advanced, usually not modified. Overrides the full Kubernetes resource name. Leave empty so the release name controls resource names.",
      "default": "",
      "x-supernodeCategory": "advanced"
    },
    "displayName": {
      "type": "string",
      "description": "Optional. Human-readable workload name shown by Supernode tooling. MCP normally sets this from releaseName.",
      "default": "",
      "x-supernodeCategory": "optional"
    },
    "image": {
      "type": "object",
      "description": "Optional. Cardano node container image settings. Most installs only change image.tag when pinning a specific Cardano node release.",
      "x-supernodeCategory": "optional",
      "additionalProperties": false,
      "properties": {
        "repository": {
          "type": "string",
          "description": "Advanced, usually not modified. Container image repository for cardano-node.",
          "default": "ghcr.io/blinklabs-io/cardano-node"
        },
        "tag": {
          "type": "string",
          "description": "Optional. Cardano node image tag to deploy.",
          "default": "11.0.1"
        }
      }
    },
    "resources": {
      "type": "object",
      "description": "Optional. Kubernetes CPU and memory requests and limits for the relay container. Recommended limits are 4 CPU cores and 8Gi memory for mainnet, and 2 CPU cores and 4Gi memory for preview or preprod. Request little CPU, usually 500m, and set memory requests equal to memory limits.",
      "x-supernodeCategory": "optional",
      "additionalProperties": false,
      "properties": {
        "requests": { "$ref": "#/definitions/resourceList" },
        "limits": { "$ref": "#/definitions/resourceList" }
      }
    },
    "service": {
      "type": "object",
      "description": "Optional. Kubernetes Service settings. Keep ClusterIP for in-cluster use; use LoadBalancer only when external node-to-node access is required.",
      "x-supernodeCategory": "optional",
      "additionalProperties": false,
      "properties": {
        "type": { "type": "string", "enum": ["ClusterIP", "NodePort", "LoadBalancer"], "default": "ClusterIP" },
        "annotations": { "$ref": "#/definitions/stringMap" },
        "labels": { "$ref": "#/definitions/stringMap" },
        "clusterIP": { "type": "string", "default": "" },
        "loadBalancerIP": { "type": "string", "default": "" },
        "externalTrafficPolicy": { "type": "string", "enum": ["", "Cluster", "Local"], "default": "" },
        "n2nPort": { "type": "integer", "description": "Advanced, usually not modified. Node-to-node Service port.", "default": 3000 },
        "n2nNodePort": { "type": ["integer", "null"], "default": null },
        "n2cPort": { "type": "integer", "description": "Advanced, usually not modified. Node-to-client Service port exposed through the proxy sidecar.", "default": 3307 },
        "n2cNodePort": { "type": ["integer", "null"], "default": null },
        "metricsPort": { "type": "integer", "description": "Advanced, usually not modified. Metrics Service port.", "default": 12798 },
        "metricsNodePort": { "type": ["integer", "null"], "default": null }
      }
    },
    "persistence": {
      "type": "object",
      "description": "Required. Persistent storage for the Cardano chain database. Persistence is always enabled for this chart.",
      "x-supernodeCategory": "required",
      "required": ["storageClass"],
      "additionalProperties": false,
      "properties": {
        "storageClass": { "type": "string", "description": "Required. Kubernetes StorageClass used for the chain-data PVC.", "minLength": 1, "x-supernodeCategory": "required", "x-supernodeRole": "storageClass" },
        "accessModes": { "type": "array", "description": "Advanced, usually not modified. PVC access modes.", "items": { "type": "string" }, "default": ["ReadWriteOnce"] },
        "annotations": { "$ref": "#/definitions/stringMap" },
        "size": { "type": "string", "description": "Optional. Requested chain database PVC size. Use roughly 250Gi for mainnet and 80Gi to 120Gi for preview or preprod.", "pattern": "^[0-9]+(Mi|Gi|Ti)$", "default": "80Gi" },
        "existingClaim": { "type": "string", "description": "Advanced, usually not modified. Existing PVC name to mount instead of creating a StatefulSet volumeClaimTemplate.", "default": "" }
      }
    },
    "node": {
      "type": "object",
      "description": "Required. Cardano relay runtime settings. Set node.network and usually leave the low-level ports and topology defaults unchanged.",
      "x-supernodeCategory": "required",
      "required": ["network"],
      "additionalProperties": false,
      "properties": {
        "replicas": { "type": "integer", "description": "Advanced, usually not modified. Number of relay pods.", "minimum": 1, "default": 1 },
        "network": { "type": "string", "description": "Required. Cardano network to join. The chart derives network magic internally for preview and preprod.", "enum": ["mainnet", "preprod", "preview"], "default": "preview", "x-supernodeCategory": "required" },
        "n2nPort": { "type": "integer", "description": "Advanced, usually not modified. Internal cardano-node node-to-node port.", "default": 3000 },
        "metricsPort": { "type": "integer", "description": "Advanced, usually not modified. Internal cardano-node metrics port.", "default": 12798 },
        "socketPath": { "type": "string", "description": "Advanced, usually not modified. Cardano node socket path shared with the proxy sidecar.", "default": "/ipc/node.socket" },
        "topology": { "$ref": "#/definitions/topology" }
      }
    },
    "proxy": {
      "type": "object",
      "description": "Advanced, usually not modified. Node-to-client TCP proxy settings. The proxy is always enabled and uses nginx with fixed chart-owned configuration.",
      "x-supernodeCategory": "advanced",
      "additionalProperties": false,
      "properties": {
        "n2cPort": { "type": "integer", "description": "Internal proxy listener port for node-to-client traffic.", "default": 3307 }
      }
    },
    "nodeSelector": { "type": "object", "description": "Advanced, usually not modified. Kubernetes nodeSelector for relay scheduling.", "additionalProperties": { "type": "string" }, "default": {}, "x-supernodeCategory": "advanced" },
    "tolerations": { "type": "array", "description": "Advanced, usually not modified. Kubernetes tolerations for relay scheduling.", "items": { "type": "object", "additionalProperties": true }, "default": [], "x-supernodeCategory": "advanced" },
    "affinity": { "type": "object", "description": "Advanced, usually not modified. Kubernetes affinity rules for relay pod placement.", "additionalProperties": true, "default": {}, "x-supernodeCategory": "advanced" },
    "topologySpreadConstraints": { "type": "array", "description": "Advanced, usually not modified. Kubernetes topology spread constraints.", "items": { "type": "object", "additionalProperties": true }, "default": [], "x-supernodeCategory": "advanced" }
  },
  "definitions": {
    "resourceList": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "cpu": { "type": "string", "description": "Kubernetes CPU quantity. Use 500m for requests; use 4 for mainnet limits and 2 for preview or preprod limits." },
        "memory": { "type": "string", "description": "Kubernetes memory quantity. Set requests.memory equal to limits.memory: 8Gi for mainnet, 4Gi for preview or preprod." }
      }
    },
    "stringMap": { "type": "object", "additionalProperties": { "type": "string" } },
    "topology": {
      "type": "object",
      "description": "Optional. Relay topology configuration. Leave mode=image-default for normal public relay behavior. Use relay-service to peer with specific in-cluster relay Services or custom for explicit topology.json roots.",
      "additionalProperties": false,
      "properties": {
        "mode": { "type": "string", "enum": ["image-default", "relay-service", "custom"], "default": "image-default" },
        "relayTargets": { "type": "array", "description": "Internal relay services to use when mode=relay-service.", "items": { "$ref": "#/definitions/relayTarget" }, "default": [] },
        "localRoots": { "type": "array", "description": "Custom topology localRoots when mode=custom.", "items": { "type": "object", "additionalProperties": true }, "default": [] },
        "publicRoots": { "type": "array", "description": "Custom topology publicRoots when mode=custom.", "items": { "type": "object", "additionalProperties": true }, "default": [] },
        "useLedgerAfterSlot": { "type": "integer", "description": "Topology useLedgerAfterSlot value. Public relays typically use 0.", "default": 0 }
      }
    },
    "relayTarget": {
      "type": "object",
      "required": ["releaseName"],
      "additionalProperties": false,
      "properties": {
        "releaseName": { "type": "string", "minLength": 1 },
        "namespace": { "type": "string", "minLength": 1 },
        "chart": { "type": "string", "minLength": 1, "default": "cardano-relay" },
        "port": { "type": "integer", "minimum": 1, "maximum": 65535, "default": 3000 },
        "valency": { "type": "integer", "minimum": 1, "default": 1 }
      }
    }
  }
}
