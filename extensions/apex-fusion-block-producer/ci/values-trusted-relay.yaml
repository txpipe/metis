persistence:
  storageClass: standard

relayPersistence:
  storageClass: standard

node:
  network: vector-testnet

blockProducer:
  poolId: pool1exampleexampleexampleexampleexampleexampleexampleexample
  vaultStaticSecret:
    path: runtime/apex-fusion/example/block-producer

relays:
  count: 0
  trusted:
    - address: vector-relay.apex.svc.cluster.local
      port: 3000
      valency: 1
