{{- if .Values.supernodeMcp.enabled }}
{{- if .Values.supernodeMcp.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.supernodeMcp.serviceAccount.name }}
  namespace: '{{ .Values.namespace }}'
  labels:
    app.kubernetes.io/name: supernode-mcp
    app.kubernetes.io/component: mcp
{{- end }}
{{- end }}
