apiVersion: v1
kind: Service
metadata:
  name: supernode-dashboard
  namespace: '{{ .Values.namespace }}'
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: supernode-dashboard
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    nodePort: 30300
    protocol: TCP
