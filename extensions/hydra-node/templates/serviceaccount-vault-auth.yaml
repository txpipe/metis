{{- if .Values.keys.vaultAuth.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.keys.vaultAuth.serviceAccount.name | quote }}
  labels:
    {{- include "hydra-node.labels" . | nindent 4 }}
{{- end }}
