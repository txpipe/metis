{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "title": "Dolos Helm Values",
  "description": "Supernode-opinionated Dolos chart values. This schema is the public configuration interface for LLMs and MCP clients; pass values in this shape directly to Helm instead of translating from a separate extension-specific object.",
  "type": "object",
  "required": ["dolos", "config", "persistence"],
  "additionalProperties": false,
  "properties": {
    "nameOverride": {
      "type": "string",
      "description": "Advanced, usually not modified. Overrides the Kubernetes app name used by the chart. Leave empty unless there is a naming collision.",
      "default": "",
      "x-supernodeCategory": "advanced"
    },
    "fullnameOverride": {
      "type": "string",
      "description": "Advanced, usually not modified. Overrides the full Kubernetes resource name. Leave empty so the release name controls resource names.",
      "default": "",
      "x-supernodeCategory": "advanced"
    },
    "displayName": {
      "type": "string",
      "description": "Optional. Human-readable workload name shown by Supernode tooling. MCP normally sets this from releaseName.",
      "default": "",
      "x-supernodeCategory": "optional"
    },
    "image": {
      "type": "object",
      "description": "Optional. Dolos container image settings. Most installs only change image.tag when pinning or testing a specific Dolos release.",
      "x-supernodeCategory": "optional",
      "additionalProperties": false,
      "properties": {
        "repository": {
          "type": "string",
          "description": "Advanced, usually not modified. Container image repository for Dolos.",
          "default": "ghcr.io/txpipe/dolos"
        },
        "tag": {
          "type": "string",
          "description": "Optional. Dolos image tag to deploy. Keep the chart default unless a specific Dolos version is required.",
          "default": "v1.1.1"
        }
      }
    },
    "resources": {
      "type": "object",
      "description": "Optional. Kubernetes CPU and memory requests and limits for both the Dolos container and bootstrap init container. Recommended limits are 4 CPU cores and 8Gi memory for cardano-mainnet, and 2 CPU cores and 4Gi memory for cardano-preview or cardano-preprod. Request little CPU, usually 500m, so scheduling stays flexible while allowing bursts up to the CPU limit. Memory requests should match memory limits because Dolos needs predictable memory and Kubernetes may evict pods whose memory request is lower than actual steady-state use.",
      "x-supernodeCategory": "optional",
      "additionalProperties": false,
      "properties": {
        "requests": {
          "$ref": "#/definitions/resourceList",
          "description": "Optional. Scheduler reservation for Dolos CPU and memory. Use cpu 500m for all networks. Set memory equal to limits.memory: 8Gi for cardano-mainnet and 4Gi for cardano-preview or cardano-preprod."
        },
        "limits": {
          "$ref": "#/definitions/resourceList",
          "description": "Optional. Maximum CPU and memory available to Dolos. Use cpu 4 and memory 8Gi for cardano-mainnet; use cpu 2 and memory 4Gi for cardano-preview or cardano-preprod."
        }
      }
    },
    "service": {
      "type": "object",
      "description": "Optional. Kubernetes Service settings for Dolos API exposure. Use ClusterIP for in-cluster access; use LoadBalancer only when external access is required.",
      "x-supernodeCategory": "optional",
      "additionalProperties": false,
      "properties": {
        "type": {
          "type": "string",
          "description": "Optional. Kubernetes Service type. ClusterIP keeps Dolos internal to the cluster. LoadBalancer exposes the service externally when the cluster supports it.",
          "enum": ["ClusterIP", "NodePort", "LoadBalancer"],
          "default": "ClusterIP"
        },
        "annotations": {
          "$ref": "#/definitions/stringMap",
          "description": "Advanced, usually not modified. Additional annotations for cloud load balancer integrations."
        },
        "labels": {
          "$ref": "#/definitions/stringMap",
          "description": "Advanced, usually not modified. Additional labels on the Service resource."
        },
        "clusterIP": {
          "type": "string",
          "description": "Advanced, usually not modified. Explicit ClusterIP value. Leave empty for Kubernetes to allocate one.",
          "default": ""
        },
        "loadBalancerIP": {
          "type": "string",
          "description": "Advanced, usually not modified. Requested static load balancer IP, if supported by the cluster provider.",
          "default": ""
        },
        "externalTrafficPolicy": {
          "type": "string",
          "description": "Advanced, usually not modified. External traffic policy for NodePort or LoadBalancer services. Leave empty unless the cluster networking policy requires Local or Cluster.",
          "enum": ["", "Cluster", "Local"],
          "default": ""
        },
        "grpcPort": {
          "type": "integer",
          "description": "Advanced, usually not modified. Service and container port for UTxO RPC gRPC.",
          "default": 50051
        },
        "grpcNodePort": {
          "type": ["integer", "null"],
          "description": "Advanced, usually not modified. Explicit NodePort for grpc when service.type is NodePort or LoadBalancer. Leave null for Kubernetes allocation.",
          "default": null
        },
        "ouroborosPort": {
          "type": "integer",
          "description": "Advanced, usually not modified. Internal Dolos ouroboros container port. This port is not exposed on the Service.",
          "default": 30013
        },
        "ouroborosNodePort": {
          "type": ["integer", "null"],
          "description": "Advanced, usually not modified. Reserved for compatibility with values consumers; the opinionated Service does not expose ouroboros.",
          "default": null
        },
        "minibfPort": {
          "type": "integer",
          "description": "Advanced, usually not modified. Service and container port for the Blockfrost-compatible minibf HTTP API.",
          "default": 3001
        },
        "minibfNodePort": {
          "type": ["integer", "null"],
          "description": "Advanced, usually not modified. Explicit NodePort for minibf when service.type is NodePort or LoadBalancer. Leave null for Kubernetes allocation.",
          "default": null
        },
        "minikupoPort": {
          "type": "integer",
          "description": "Advanced, usually not modified. Service and container port for the Kupo-compatible minikupo HTTP API.",
          "default": 1442
        },
        "minikupoNodePort": {
          "type": ["integer", "null"],
          "description": "Advanced, usually not modified. Explicit NodePort for minikupo when service.type is NodePort or LoadBalancer. Leave null for Kubernetes allocation.",
          "default": null
        },
        "trpPort": {
          "type": "integer",
          "description": "Advanced, usually not modified. Service and container port for the Dolos TRP HTTP API.",
          "default": 8164
        },
        "trpNodePort": {
          "type": ["integer", "null"],
          "description": "Advanced, usually not modified. Explicit NodePort for trp when service.type is NodePort or LoadBalancer. Leave null for Kubernetes allocation.",
          "default": null
        }
      }
    },
    "persistence": {
      "type": "object",
      "description": "Required. Persistent storage for the Dolos chain database. At minimum set persistence.storageClass for Supernode installs.",
      "x-supernodeCategory": "required",
      "required": ["storageClass"],
      "additionalProperties": false,
      "properties": {
        "storageClass": {
          "type": "string",
          "description": "Required. Kubernetes StorageClass used for the Dolos data PVC. Choose a StorageClass available in the target cluster.",
          "minLength": 1,
          "x-supernodeCategory": "required",
          "x-supernodeRole": "storageClass"
        },
        "accessModes": {
          "type": "array",
          "description": "Advanced, usually not modified. PVC access modes. ReadWriteOnce is appropriate for the single-replica StatefulSet.",
          "items": { "type": "string" },
          "default": ["ReadWriteOnce"]
        },
        "annotations": {
          "$ref": "#/definitions/stringMap",
          "description": "Advanced, usually not modified. Annotations added to the generated Dolos data PVC."
        },
        "size": {
          "type": "string",
          "description": "Optional. Requested PVC size for the Dolos chain database. Use larger values for mainnet; 50Gi is suitable for preview and preprod defaults.",
          "pattern": "^[0-9]+(Mi|Gi|Ti)$",
          "default": "20Gi"
        },
        "existingClaim": {
          "type": "string",
          "description": "Advanced, usually not modified. Existing PVC name to mount instead of creating a StatefulSet volumeClaimTemplate. Leave empty for normal installs.",
          "default": ""
        }
      }
    },
    "dolos": {
      "type": "object",
      "description": "Required. Dolos runtime settings. For normal Supernode installs, set dolos.network and leave command, args, and bootstrap defaults unchanged.",
      "x-supernodeCategory": "required",
      "required": ["network"],
      "additionalProperties": false,
      "properties": {
        "replicas": {
          "type": "integer",
          "description": "Advanced, usually not modified. Number of Dolos StatefulSet replicas. Keep 1 because the chart provisions a single writable data volume per pod and Supernode expects one Dolos instance per release.",
          "minimum": 0,
          "default": 1
        },
        "network": {
          "type": "string",
          "description": "Required. Cardano network Dolos should index. This selects the built-in Dolos configuration preset unless config.customConfig is provided.",
          "enum": ["cardano-mainnet", "cardano-preprod", "cardano-preview"],
          "default": "cardano-preview",
          "x-supernodeCategory": "required"
        },
        "command": {
          "type": "array",
          "description": "Advanced, usually not modified. Overrides the Dolos container command. Leave empty to use the image entrypoint.",
          "items": { "type": "string" },
          "default": []
        },
        "args": {
          "type": "array",
          "description": "Advanced, usually not modified. Dolos daemon arguments. The default runs `dolos -c /etc/config/dolos.toml daemon`.",
          "items": { "type": "string" },
          "default": ["-c", "/etc/config/dolos.toml", "daemon"]
        },
        "bootstrap": {
          "type": "object",
          "description": "Optional. Bootstrap init container settings. Keep enabled for normal installs so Dolos initializes data before the daemon starts.",
          "additionalProperties": false,
          "properties": {
            "enabled": {
              "type": "boolean",
              "description": "Optional. Enables the bootstrap init container. Disable only when reusing an already initialized data volume.",
              "default": true
            },
            "name": {
              "type": "string",
              "description": "Advanced, usually not modified. Name of the bootstrap init container.",
              "default": "bootstrap"
            },
            "command": {
              "type": "array",
              "description": "Advanced, usually not modified. Overrides the bootstrap container command. Leave empty to use the image entrypoint.",
              "items": { "type": "string" },
              "default": []
            },
            "args": {
              "type": "array",
              "description": "Advanced, usually not modified. Bootstrap arguments. Leave empty to use the chart default full snapshot bootstrap command.",
              "items": { "type": "string" },
              "default": []
            }
          }
        }
      }
    },
    "config": {
      "type": "object",
      "description": "Required. Dolos TOML configuration input. The chart always creates a ConfigMap mounted at /etc/config/dolos.toml.",
      "x-supernodeCategory": "required",
      "required": ["upstreamAddress"],
      "additionalProperties": false,
      "properties": {
        "customConfig": {
          "type": "string",
          "description": "Optional. Full Dolos TOML configuration. Leave empty to use the chart preset selected by dolos.network. If set, the custom content is templated by Helm and written to /etc/config/dolos.toml.",
          "default": ""
        },
        "upstreamAddress": {
          "type": "string",
          "description": "Required. Trusted Cardano relay address Dolos uses for syncing, in host:port form. MCP does not auto-resolve this value. If the address is unknown, run workloads.list and inspect same-network Cardano relay workloads; a typical candidate is RELEASE_FULLNAME.NAMESPACE.svc.cluster.local:PORT, where PORT is usually the relay n2n port such as 3000.",
          "minLength": 1,
          "x-supernodeCategory": "required"
        },
        "extraFiles": {
          "type": "object",
          "description": "Advanced, usually not modified. Additional templated files to include in the Dolos config ConfigMap.",
          "additionalProperties": { "type": "string" },
          "default": {}
        }
      }
    },
    "nodeSelector": {
      "type": "object",
      "description": "Advanced, usually not modified. Kubernetes nodeSelector for scheduling Dolos onto specific nodes. Use only when the cluster has known node labels for storage or workload placement.",
      "additionalProperties": { "type": "string" },
      "default": {},
      "x-supernodeCategory": "advanced"
    },
    "tolerations": {
      "type": "array",
      "description": "Advanced, usually not modified. Kubernetes tolerations for scheduling Dolos onto tainted nodes. Only set when the target cluster requires specific taints.",
      "items": { "type": "object", "additionalProperties": true },
      "default": [],
      "x-supernodeCategory": "advanced"
    },
    "affinity": {
      "type": "object",
      "description": "Advanced, usually not modified. Kubernetes affinity rules for Dolos pod placement. Leave empty unless there is a cluster-specific scheduling requirement.",
      "additionalProperties": true,
      "default": {},
      "x-supernodeCategory": "advanced"
    },
    "topologySpreadConstraints": {
      "type": "array",
      "description": "Advanced, usually not modified. Kubernetes topology spread constraints. Mostly useful for cluster operators with explicit zone or node spreading policies.",
      "items": { "type": "object", "additionalProperties": true },
      "default": [],
      "x-supernodeCategory": "advanced"
    }
  },
  "definitions": {
    "resourceList": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "cpu": {
          "type": "string",
          "description": "Kubernetes CPU quantity. For Dolos requests use 500m. For Dolos limits use 4 on cardano-mainnet and 2 on cardano-preview or cardano-preprod."
        },
        "memory": {
          "type": "string",
          "description": "Kubernetes memory quantity. For Dolos, requests.memory should match limits.memory: 8Gi on cardano-mainnet and 4Gi on cardano-preview or cardano-preprod."
        }
      }
    },
    "stringMap": {
      "type": "object",
      "additionalProperties": { "type": "string" }
    }
  }
}
