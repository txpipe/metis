import { Card, CardBody, CardHeader } from '~/components/ui/Card';

export function TabResources(_: { item: CatalogItem; }) {
  return (
    <Card className="mt-9">
      <CardHeader>Resources</CardHeader>
      <CardBody className="flex flex-col gap-6 text-zinc-800">
        Resources gg
      </CardBody>
    </Card>
  );
}
